import React from 'react';
import Layout from '../components/Layout';
import LayoutContainer from '../containers/container-layout';

export default (props) => <LayoutContainer {...props} component={Layout} />;

